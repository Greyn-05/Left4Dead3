using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class HP : MonoBehaviour
{
    // 체력바 업데잍느 함수
    // private void UpdateHP()
    // {
    //     hpSlider.value = Mathf.Lerp(hpSlider.value, CurrentHP, Time.deltaTime * 10);
    // }
}
